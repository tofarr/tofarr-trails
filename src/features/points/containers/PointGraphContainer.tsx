import React, { FC } from 'react';

import IPoint from '../IPoint';

interface IMsgsProps{
  hike_id: number;
}

const PointGraphContainer: FC<IMsgsProps> = ({ hike_id }) => (
  <div>TODO: Point Graph</div>
);

export default PointGraphContainer;
